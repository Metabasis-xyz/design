Square icon-only toolbar button; children is a 16–20px Lucide SVG at stroke 1.5.

```jsx
<IconButton label="Settings" size="sm"><SettingsIcon /></IconButton>
```

`active` marks a toggled state with accent-subtle fill. Always pass `label`.
