Flat underline tabs — 2px accent underline on the active item, no pill/segmented styles.

```jsx
<Tabs items={[{id:"pos",label:"Positions"},{id:"ord",label:"Orders"}]} value={tab} onChange={setTab} size="sm" />
```
